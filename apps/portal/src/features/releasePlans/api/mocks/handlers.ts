// Mock handlers for msw (kept minimal to avoid unused import errors during typecheck)
export const handlers: any[] = [];
// Example (uncomment when using MSW):
// import { http, HttpResponse } from 'msw';
// export const handlers = [
//   http.get('/api/release-plans', () => HttpResponse.json([])),
// ];
