# 📊 Builder Pattern Architecture - Visual Guide

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    ComponentsTab.tsx                             │
│                   (Visual Component)                             │
├─────────────────────────────────────────────────────────────────┤
│  Before: 60+ lines of logic                                      │
│  After:  3 lines → buildComponentConfig()                       │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ↓ import
┌─────────────────────────────────────────────────────────────────┐
│              @/constants/componentConfig.ts                      │
│              (Builder & Configuration Layer)                     │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ buildComponentConfig(componentName: string)              │  │
│  │   ↓ Searches COMPONENT_TYPE_MAP                          │  │
│  │   ↓ Matches keywords                                     │  │
│  │   ↓ Returns ComponentConfig                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                          ↑                                       │
│                    uses map from                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ COMPONENT_TYPE_MAP                                        │  │
│  │ ├─ web: { keywords, icon, color, description }          │  │
│  │ ├─ mobile: { ... }                                       │  │
│  │ ├─ service: { ... }                                      │  │
│  │ ├─ dashboard: { ... }                                    │  │
│  │ ├─ gateway: { ... }                                      │  │
│  │ └─ default: { ... }                                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                     ↑
                     │ uses
┌─────────────────────────────────────────────────────────────────┐
│              @/constants/index.ts                                │
│              (Public Exports)                                    │
├─────────────────────────────────────────────────────────────────┤
│  export {                                                         │
│    buildComponentConfig,                                         │
│    getAvailableComponentTypes,                                  │
│    type ComponentConfig                                          │
│  } from "./componentConfig";                                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow

```
Input: "User Portal"
  │
  ↓
┌─────────────────────────────────────────────────────────────────┐
│ buildComponentConfig("User Portal")                              │
├─────────────────────────────────────────────────────────────────┤
│ 1. Normalize: "user portal" (lowercase)                         │
│ 2. Search COMPONENT_TYPE_MAP entries                            │
│ 3. Match keywords:                                               │
│    - web: keywords: ["web", "portal"]  ✅ MATCH!               │
│ 4. Return config from matched entry:                            │
│    {                                                              │
│      name: "User Portal",                                        │
│      icon: <WebIcon />,                                          │
│      color: "primary",                                           │
│      description: "Frontend web application or portal"           │
│    }                                                              │
└─────────────────────────────────────────────────────────────────┘
  │
  ↓
Output: ComponentConfig object
```

---

## 🎯 Keyword Matching Flow

```
Component Name Input
        │
        ↓
   Normalize (lowercase)
        │
        ↓
    ┌───┴────────┬──────────┬──────────┬──────────┬──────────┐
    │            │          │          │          │          │
    ↓            ↓          ↓          ↓          ↓          ↓
  web?      mobile?      service?   dashboard?  gateway?   default?
    │            │          │          │          │          │
  Check       Check       Check      Check       Check       Use
keywords    keywords    keywords   keywords    keywords    Default
    │            │          │          │          │          │
    ↓            ↓          ↓          ↓          ↓          ↓
  ["web"      ["mobile",  ["service", ["dashboard"] ["gateway"] DATABASE
  "portal"]    "app"]    "api"]      ICON
                          │          │
   RETURN CONFIG     RETURN CONFIG   ETC...
```

---

## 📦 Component Type Map Detail

```
┌──────────────────────────────────────────────────────────────┐
│              COMPONENT_TYPE_MAP                               │
├──────────────────────────────────────────────────────────────┤
│
│ ┌─ WEB ──────────────────────────────────────────────────┐
│ │ keywords: ["web", "portal"]                            │
│ │ iconComponent: WebIcon                                 │
│ │ color: "primary"                                       │
│ │ description: "Frontend web application or portal"      │
│ └────────────────────────────────────────────────────────┘
│
│ ┌─ MOBILE ───────────────────────────────────────────────┐
│ │ keywords: ["mobile", "app"]                            │
│ │ iconComponent: MobileIcon                              │
│ │ color: "secondary"                                     │
│ │ description: "Mobile application"                      │
│ └────────────────────────────────────────────────────────┘
│
│ ┌─ SERVICE ───────────────────────────────────────────────┐
│ │ keywords: ["service", "api"]                           │
│ │ iconComponent: ServiceIcon                             │
│ │ color: "success"                                       │
│ │ description: "Backend service or API"                  │
│ └────────────────────────────────────────────────────────┘
│
│ ┌─ DASHBOARD ────────────────────────────────────────────┐
│ │ keywords: ["dashboard"]                                │
│ │ iconComponent: PortalIcon                              │
│ │ color: "info"                                          │
│ │ description: "Dashboard or analytics interface"        │
│ └────────────────────────────────────────────────────────┘
│
│ ┌─ GATEWAY ──────────────────────────────────────────────┐
│ │ keywords: ["gateway"]                                  │
│ │ iconComponent: ApiIcon                                 │
│ │ color: "warning"                                       │
│ │ description: "API Gateway or routing service"          │
│ └────────────────────────────────────────────────────────┘
│
│ ┌─ DEFAULT (fallback) ───────────────────────────────────┐
│ │ keywords: [] (never matches)                           │
│ │ iconComponent: DatabaseIcon                            │
│ │ color: "primary"                                       │
│ │ description: "System component"                        │
│ └────────────────────────────────────────────────────────┘
│
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Before & After Comparison

### BEFORE: Inline Logic

```typescript
┌────────────────────────────────────────────────────────────┐
│ ComponentsTab.tsx (Line 39-95)                             │
├────────────────────────────────────────────────────────────┤
│ const getComponentConfig = (componentName) => {             │
│   const name = componentName.toLowerCase();                 │
│                                                              │
│   if (name.includes("web") || name.includes("portal")) {  │
│     return {                                                │
│       name: componentName,                                  │
│       icon: <WebIcon />,                                   │
│       color: "primary",                                    │
│       description: "Frontend web app..."                   │
│     };                                                      │
│   }                                                          │
│                                                              │
│   if (name.includes("mobile") || name.includes("app")) {   │
│     return { ... };                                        │
│   }                                                          │
│                                                              │
│   // ... MORE IF/ELSE BLOCKS ...                           │
│                                                              │
│   return { ... }; // default                               │
│ }                                                            │
│                                                              │
│ // Usage:                                                   │
│ const config = getComponentConfig(name);                   │
└────────────────────────────────────────────────────────────┘
         │
         ├─ ❌ Hardcoded in component
         ├─ ❌ Not reusable
         ├─ ❌ Hard to test
         ├─ ❌ Difficult to extend
         └─ ❌ Violates SRP
```

### AFTER: Builder Pattern

```typescript
┌──────────────────────────────────────────────┐
│ src/constants/componentConfig.ts             │
├──────────────────────────────────────────────┤
│ const COMPONENT_TYPE_MAP = { ... }           │
│ export function buildComponentConfig(...) {} │
│ export function getAvailableComponentTypes() │
└──────────────────────────────────────────────┘
         ↑
         │ import
         │
┌──────────────────────────────────────────────┐
│ ComponentsTab.tsx (Line 19)                  │
├──────────────────────────────────────────────┤
│ import {                                      │
│   buildComponentConfig                       │
│ } from "@/constants";                        │
│                                              │
│ // Usage:                                    │
│ const config = buildComponentConfig(name);   │
└──────────────────────────────────────────────┘
         │
         ├─ ✅ Centralized
         ├─ ✅ Reusable
         ├─ ✅ Easy to test
         ├─ ✅ Simple to extend
         └─ ✅ Follows SRP
```

---

## 🎯 Type Safety Flow

```
Input Validation
        │
        ↓
┌─────────────────────────────────────────┐
│ buildComponentConfig(                   │
│   componentName: string                 │ ← Type checked
│ ): ComponentConfig                      │ ← Return type checked
│                                         │
│ return {                                │
│   name: string;                         │ ← Typed
│   icon: React.ReactElement;             │ ← Typed
│   color: "primary" | "secondary" | ...  │ ← Union type checked
│   description: string;                  │ ← Typed
│ }                                       │
└─────────────────────────────────────────┘
        │
        ↓
Consumer gets fully typed object
```

---

## 🚀 Usage Patterns

### Pattern 1: Simple Mapping

```
components: string[]
    ↓
map(name → buildComponentConfig(name))
    ↓
configs: ComponentConfig[]
    ↓
render <ComponentCard config={config} />
```

### Pattern 2: Filtering

```
components: string[]
    ↓
filter(name → buildComponentConfig(name).color === "primary")
    ↓
filtered: string[]
```

### Pattern 3: Grouping

```
components: string[]
    ↓
group by buildComponentConfig(name).color
    ↓
groups: Record<Color, ComponentConfig[]>
```

---

## 📈 Scalability

### Current State (5 types)

```
COMPONENT_TYPE_MAP = {
  web: {...},      ─┐
  mobile: {...},    ├─ O(1) lookup per type
  service: {...},   │  O(n) iteration where n=5
  dashboard: {...}, │
  gateway: {...}   ─┘
}
```

### Future Growth

```
If need to add 10 more types:
├─ Edit: COMPONENT_TYPE_MAP only
├─ Add: 1 entry per type
└─ Result: Automatic availability in buildComponentConfig()
   No other files need changes!
```

---

## 🧪 Testing Structure

```
buildComponentConfig()
    │
    ├─ Case 1: Web components
    │   Input: "User Portal" → Output: { color: "primary", ... }
    │
    ├─ Case 2: Mobile components
    │   Input: "iOS App" → Output: { color: "secondary", ... }
    │
    ├─ Case 3: Unknown components
    │   Input: "CustomThing" → Output: { color: "primary" (default), ... }
    │
    └─ Case 4: Case insensitivity
        Input: "USER PORTAL" = "user portal" → Same config
```

---

## 🎓 Design Principles Applied

```
┌──────────────────────────────────────────────────────────┐
│ SOLID Principles                                         │
├──────────────────────────────────────────────────────────┤
│ S - Single Responsibility                                │
│     ✅ Builder handles construction only                │
│                                                           │
│ O - Open/Closed                                          │
│     ✅ Open for extension (add types)                   │
│     ✅ Closed for modification (no other files change)  │
│                                                           │
│ L - Liskov Substitution                                  │
│     ✅ ComponentConfig always has same interface       │
│                                                           │
│ I - Interface Segregation                                │
│     ✅ Exports only what's needed                       │
│                                                           │
│ D - Dependency Inversion                                 │
│     ✅ Components depend on abstract interface         │
│        not concrete implementation                       │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 Summary Chart

| Aspect            | Before            | After               |
| ----------------- | ----------------- | ------------------- |
| **Code Location** | ComponentsTab.tsx | componentConfig.ts  |
| **Lines**         | ~60               | ~130 (but reusable) |
| **Reusable**      | ❌ No             | ✅ Yes              |
| **Testable**      | ⚠️ Hard           | ✅ Easy             |
| **Extendable**    | ⚠️ Difficult      | ✅ Simple           |
| **Maintainable**  | ❌ Low            | ✅ High             |
| **Follows SOLID** | ❌ Partial        | ✅ Full             |
| **Documentation** | ❌ None           | ✅ Complete         |

---

**Esta arquitectura permite que el código sea escalable, mantenible y preparado para cambios futuros.** 🎯
