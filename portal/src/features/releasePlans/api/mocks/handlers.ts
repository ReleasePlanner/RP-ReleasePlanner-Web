import { http, HttpResponse } from 'msw';

// Example handlers (replace with real endpoints when API exists)
export const handlers = [
	// http.get('/api/release-plans', () => HttpResponse.json([])),
];


